from setuptools import setup, find_packages

NAME = 'passgen'
VER = '1.0'
DESC = 'This package can generate your new password in no time!'

setup(
    name=NAME,
    version=VER,
    description=DESC,
    packages=find_packages()
)